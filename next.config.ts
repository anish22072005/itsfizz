import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // Uncomment below for static export to GitHub Pages
  // output: 'export',
  // basePath: '/itsfizz', // Replace with your repo name
};

export default nextConfig;
